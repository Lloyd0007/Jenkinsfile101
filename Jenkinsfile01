pipeline { 
agent any
environment {
   GIT_ASKPASS = credentials('ee9757fd')
}
    stages {
        //skip the build and test stages 
      stage("Deploy") {
            steps {
                echo 'Deploying the application....'
                echo "Deploying with ${GIT_ASKPASS}"
                sh "${GIT_ASKPASS}"
            }
        }
    }
}
