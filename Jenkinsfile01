pipeline { 
agent any
environment {
    JENKINS_CREDENTIALS = credentials('ee9757fd')
}
    stages {
        //skip the build and test stages 
      stage("Deploy") {
            steps {
                echo 'Deploying the application....'
                echo "Deploying with ${JENKINS_CREDENTIALS}"
            }
        }
    }
}
